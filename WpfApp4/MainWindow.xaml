<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="Plik">
                <MenuItem Header="Otwórz" Click="OpenFile_Click" />
                <MenuItem Header="_Save" Click="SaveFile_Click" IsEnabled="False" Name="MenuSave" />
                <Separator />
                <MenuItem Header="Exit" Click="Exit_Click" />
            </MenuItem>
        </Menu>

        <TextBox Name="pole" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="30" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"/>

    </Grid>
</Window>
